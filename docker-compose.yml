services:
  db-init:
    image: postgres:16-alpine
    container_name: trader_db_init
    environment:
      PGPASSWORD: ${POSTGRES_SUPERUSER_PASSWORD:-}
      PGUSER: ${POSTGRES_SUPERUSER:-postgres}
      DB_USER: ${DB_USER:-trader}
      DB_PASSWORD: ${DB_PASSWORD:-trader_secret}
      DB_NAME: ${DB_NAME:-trader_cockpit}
    volumes:
      - ./infra/db/setup.sh:/setup.sh:ro
    entrypoint: ["/bin/sh", "/setup.sh"]
    restart: "no"

  redis:
    image: redis:7-alpine
    container_name: trader_redis
    ports:
      - "6379:6379"
    volumes:
      - ${DOCKER_DATA_PATH:-d:/docker-data}/redis:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 3
    restart: unless-stopped

  data-sync:
    build:
      context: ./DataSyncService
      dockerfile: Dockerfile
    container_name: trader_data_sync
    environment:
      DATABASE_URL: postgresql://${DB_USER:-trader}:${DB_PASSWORD:-trader_secret}@host.docker.internal:5432/${DB_NAME:-trader_cockpit}
      REDIS_URL: ${REDIS_URL:-redis://redis:6379}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      DHAN_CLIENT_ID: ${DHAN_CLIENT_ID:-}
      DHAN_ACCESS_TOKEN: ${DHAN_ACCESS_TOKEN:-}
      DHAN_MAX_CONCURRENCY: ${DHAN_MAX_CONCURRENCY:-10}
      DHAN_SYMBOL_BATCH_SIZE: "10"
      SYNC_BATCH_SIZE: ${SYNC_BATCH_SIZE:-50}
      SYNC_1D_HISTORY_DAYS: ${SYNC_1D_HISTORY_DAYS:-1825}
      SYNC_1M_HISTORY_DAYS: ${SYNC_1M_HISTORY_DAYS:-365}
    ports:
      - "8001:8000"
    depends_on:
      db-init:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
    volumes:
      - ./DataSyncService/src:/app/src:ro
    restart: unless-stopped

  momentum-scorer:
    build:
      context: ./MomentumScorerService
      dockerfile: Dockerfile
    container_name: trader_momentum_scorer
    environment:
      DATABASE_URL: postgresql://${DB_USER:-trader}:${DB_PASSWORD:-trader_secret}@host.docker.internal:5432/${DB_NAME:-trader_cockpit}
      REDIS_URL: ${REDIS_URL:-redis://redis:6379}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      SCORE_LOOKBACK_BARS: ${SCORE_LOOKBACK_BARS:-200}
      SCORE_CONCURRENCY: ${SCORE_CONCURRENCY:-10}
      WEIGHT_RSI: ${WEIGHT_RSI:-0.30}
      WEIGHT_MACD: ${WEIGHT_MACD:-0.30}
      WEIGHT_ROC: ${WEIGHT_ROC:-0.25}
      WEIGHT_VOL: ${WEIGHT_VOL:-0.15}
      TREND_LOOKBACK_BARS: ${TREND_LOOKBACK_BARS:-60}
      ATR_PERIOD: ${ATR_PERIOD:-14}
      ATR_PCT_MAX: ${ATR_PCT_MAX:-5.0}
      MIN_AVG_DAILY_TURNOVER: ${MIN_AVG_DAILY_TURNOVER:-10000000}
    ports:
      - "8002:8000"
    depends_on:
      db-init:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
    restart: unless-stopped

  notebooks:
    build:
      context: ./notebooks
      dockerfile: Dockerfile
    container_name: trader_notebooks
    environment:
      DATABASE_URL: postgresql://${DB_USER:-trader}:${DB_PASSWORD:-trader_secret}@host.docker.internal:5432/${DB_NAME:-trader_cockpit}
    ports:
      - "8888:8888"
    depends_on:
      db-init:
        condition: service_completed_successfully
    volumes:
      - ./notebooks:/app/notebooks
    restart: unless-stopped
